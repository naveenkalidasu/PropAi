services:
  - type: web
    name: ai-interview-planner-frontend
    rootDir: frontend
    buildCommand: npm install && npm run build
    startCommand: npx serve -s build -l 10000
    env: node
    plan: free
